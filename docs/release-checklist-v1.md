# 体調日記アプリ 第三段: 本番公開前チェック（最終ゲート）v1

実施日: 2026-03-02 (JST)  
対象環境: ローカル (`http://127.0.0.1:8787`)

## 1. 今回の実装範囲
- 第三段の最終確認を実施（セキュリティ/表示/UX/安定性）
- 必須シナリオ10項目を通し検証
- 追加で認証・共有リンクの異常系を検証
- 以下の修正を反映:
  - `src/server.mjs`: 記録保存APIの入力バリデーション強化
  - `src/server.mjs`: 共有トークン形式チェック（32桁16進）の追加

## 2. チェックコマンド結果
- `npm run check`: PASS
- `node --check web/doctor.js`: PASS
- `node --check web/doctor-login.js`: PASS

## 3. 必須チェックシナリオ結果
- 患者記録保存（画像なし）: PASS (`200`)
- 患者記録保存（画像あり）: PASS (`200`, 画像URL発行あり)
- AI要約更新: PASS (`200`)
- 共有リンク発行: PASS (`200`)
- 医師ログイン: PASS (`200`)
- 医師ビュー表示（グラフ・画像）: PASS (`200`, timeline/images確認)
- 医師コメント保存: PASS (`200`)
- 患者側でコメント反映確認: PASS (`200`, note_id一致)
- 共有リンク失効後のアクセス確認: PASS (`410`, `share_link_inactive`)
- ログアウト後の医師ページアクセス確認: PASS (`302`, `/doctor-login` へ遷移)

## 4. セキュリティ最終確認
- 画像バリデーション:
  - `image_data_url` 非文字列: `400` を確認
  - 未対応MIME（SVG）: `400 invalid_image_data_url` を確認
- 共有トークン:
  - 不正トークン: `404 share_token_invalid` を確認
  - 失効トークン: `410 share_link_inactive / reason=revoked` を確認
  - 期限切れトークン: `410 share_link_inactive / reason=expired` を確認
- セッション:
  - 無効cookie: `401 doctor_auth_invalid_session` + cookie削除を確認
  - 期限切れsession: `401 doctor_session_expired` + cookie削除を確認
  - ログイン試行制限: 6回目で `429 too_many_login_attempts` + `Retry-After` を確認

## 5. 個人情報/医療文脈チェック
- 免責文言:
  - 患者画面: `web/index.html` のトップに表示あり
  - 医師画面: `web/doctor.html` のトップに表示あり
- 「診断確定ではない」方針:
  - 患者/医師両画面の固定文言で維持されていることを確認
- レスポンス/ログのPII簡易確認:
  - `password` 文字列が export/auditLogs に混入していないことを確認

## 6. UX最終確認
- 初回導線:
  - Step案内、記録完了状態、共有リンク操作導線を確認
- 失敗時メッセージ:
  - 主要エラー（認証切れ、共有リンク無効、画像不正）の日本語メッセージ確認
- 画像表示:
  - 医師ビューのサムネイル表示/モーダル拡大/閉じる（ボタン・背景クリック・Esc）の実装確認
  - 注意: この実施はローカル実装確認中心で、実ブラウザ横断の手動実機確認は別途必要

## 7. パフォーマンス・安定性簡易確認
- 連続保存20回: 失敗 `0`
- 共有リンク連続発行20回: 失敗 `0`
- 画像付き複数投入後の医師ビュー: timeline/images整合を確認
- サーバー再起動後データ保持: 保存済み記録が復元されることを確認

## 8. 既知の制約（要対応）
- 重大:
  - 患者向けAPI（`/api/v1/logs/*`, `/api/v1/profile`, `/api/v1/user/export`, `/api/v1/patient/doctor-notes`, `/api/v1/share-links` など）にユーザー認証/認可がなく、`user_id` 推測で他者データへアクセス可能
- 中:
  - `safetyEvents.raw_input` に自由記述が保存されるため、運用次第でセンシティブ情報が長期保存される
  - 画像モーダルの「全ブラウザ実機確認」は未完（実装確認まで）
- 軽微:
  - デモ用認証情報がUIに表示される（公開環境では削除必須）

## 9. 推奨運用手順
- デモ時:
  - デモ専用データセットを用意して開始前に初期化
  - セッション切れ時の再ログイン導線をリハーサル
  - 共有リンクはデモ終了時に必ず失効
- 公開時:
  - 最優先で患者認証/認可を実装（重大制約の解消）
  - ログ保存ポリシー（保存期間・マスキング）を運用設計
  - デモ資格情報の除去、環境変数/秘密情報管理を本番設定へ移行

## 10. 公開可否判断
- 判定: **条件付き可**
- 条件:
  - 対外公開前に「患者APIの認証/認可」を必須実装すること
  - ブラウザ横断の画像モーダル実機確認を完了すること
